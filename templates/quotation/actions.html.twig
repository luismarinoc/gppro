{% macro quotation(quotation, view) %}
    {% import "macros/widgets.html.twig" as widgets %}
    {% set event = actions(app.user, 'quotation', view, {'quotation': quotation}) %}
    {{ widgets.table_actions(event.actions) }}
{% endmacro %}
