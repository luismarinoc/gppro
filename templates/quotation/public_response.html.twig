<!doctype html>
<html lang="{{ app.request.locale }}">
<head><meta charset="utf-8"><title>{{ 'quotation.response.recorded_title'|trans }}</title></head>
<body>
    <h1>{{ 'quotation.response.thank_you'|trans }}</h1>
    <p>{{ 'quotation.response.recorded_message'|trans({'%status%': ('quotation.status.' ~ response)|trans}) }}</p>
</body>
</html>
