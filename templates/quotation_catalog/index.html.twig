{% extends 'base.html.twig' %}

{% block main %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Catálogo de presupuestos</h3>
            <div class="card-actions">
                <a class="btn btn-primary" href="{{ path('admin_quotation_catalog_create') }}">Agregar ítem al catálogo</a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-vcenter card-table">
                <thead><tr><th>Nombre</th><th>Unidad</th><th>Precio predeterminado</th><th>Estado</th><th></th></tr></thead>
                <tbody>
                {% for item in items %}
                    <tr>
                        <td>{{ item.name }}</td>
                        <td>{{ item.unit }}</td>
                        <td>{{ item.defaultPrice }}</td>
                        <td>{{ item.active ? 'Activo' : 'Inactivo' }}</td>
                        <td class="text-end">
                            <a href="{{ path('admin_quotation_catalog_edit', {id: item.id}) }}">Editar</a>
                            {% if item.active %}
                                <form class="d-inline" method="post" action="{{ path('admin_quotation_catalog_delete', {id: item.id}) }}">
                                    <input type="hidden" name="_token" value="{{ csrf_token('quotation_catalog_item') }}">
                                    <button class="btn btn-link text-danger" type="submit">Desactivar</button>
                                </form>
                            {% endif %}
                        </td>
                    </tr>
                {% else %}
                    <tr><td colspan="5">No hay ítems en el catálogo.</td></tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
