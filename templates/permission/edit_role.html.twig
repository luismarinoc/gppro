{% extends gppro_context.modalRequest ? 'form.html.twig' : 'base.html.twig' %}

{% block main %}
    {{ include(gppro_context.modalRequest ? 'default/_form_modal.html.twig' : 'default/_form.html.twig', {
        'title': (role.id ? 'edit'|trans : 'create'|trans) ~ ': ' ~ 'user_role.title'|trans,
        'form': form,
        'back': path('admin_user_permissions')
    }) }}
{% endblock %}
